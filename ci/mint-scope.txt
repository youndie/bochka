# Why each mint failure is there. `<status> <substring of the case name or function> <reason>`.
#
# Three statuses, and the difference between them is the whole value of this file:
#
#   out-of-scope  the case asks for something S3 does not have — MinIO's admin API, its own
#                 extensions, or a signature version S3 has retired. Passing it would mean
#                 implementing somebody else's product.
#   deferred      S3 has it, we do not, and there is a task
#   defect        our answer is wrong, and there is a task
#
# A rule that stops matching is reported as STALE rather than ignored: a reason for a failure that
# no longer happens reads as true to the next person.
#
# Those three words are the whole vocabulary, and `ci/mint_scope.py` refuses any other by name
# rather than taking it: the status is the claim, and a word the script does not know is a claim it
# cannot check. Measured before the refusal existed: a rule reading `out-of-scop` was counted as
# an explanation and the run exited 0 with the typo printed where a verdict belongs.

# --- MinIO's own surface, which is not S3 ------------------------------------------------------
out-of-scope healthcheck GET /minio/health/live is MinIO's liveness path; ours is /-/healthy and is documented as such
out-of-scope listUsers the MinIO admin API (users, groups, canned policies) is not part of S3
out-of-scope deleteUser the MinIO admin API is not part of S3
out-of-scope getUserInfo the MinIO admin API is not part of S3
out-of-scope addUser the MinIO admin API is not part of S3
out-of-scope addCannedPolicy MinIO's admin canned policies are not part of S3
out-of-scope setPolicy MinIO's admin policy API, distinct from S3 bucket policies, which are implemented (M29)
out-of-scope listCannedPolicies MinIO's admin canned policies are not part of S3
out-of-scope removeCannedPolicy MinIO's admin canned policies are not part of S3
out-of-scope listenBucketNotification MinIO's notification stream is an extension; S3 has no listen call
out-of-scope snowball snowball is MinIO's upload extension: a tar arrives with a header asking the server to unpack it, and S3 stores the tar
out-of-scope testFunctionalV2 signature version 2, which S3 retired; this server implements v4 only
out-of-scope s3select S3 Select is out of scope for v1 and answered NotImplemented on purpose
out-of-scope selectObjectContent S3 Select is out of scope for v1 and answered NotImplemented on purpose
out-of-scope testSelectObject S3 Select through the Go SDK: the same decision, and it arrives as 501 on purpose
out-of-scope abortMultipartupload mint expects an abort of an upload that does not exist to succeed; S3 documents NoSuchUpload, which is what this server answers

# --- our answer is wrong: tasks ----------------------------------------------------------------
defect head-object a copy loses the storage class: it is stored on a write (M-301) and CopyObject does not carry it, so a HEAD of the copy answers without the header (M-311)
out-of-scope minio-js-fd- what the client asked MinIO's `x-minio-force-delete` to remove is still there, so the listing after it is not empty: without that header a DELETE in a versioned bucket leaves a tombstone and the versions, which is S3
defect test_presigned_post_policy_error a POST form addressed to an object key rather than to the bucket answers NotImplemented where S3 answers 405 MethodNotAllowed (M-309)
defect testListObjectVersions `version-id-marker` without `key-marker` is accepted and answered with a listing, where S3 refuses it with InvalidArgument (M-310)
out-of-scope removeBucket the bucket still holds versions, because the client asked to remove them with MinIO's force-delete header rather than by version id
out-of-scope after all the bucket still holds versions, because the client asked to remove them with MinIO's force-delete header rather than by version id
