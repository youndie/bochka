# Why a ceph/s3-tests case fails, and what that means for bochka.
#
# One rule per line: <status> <tab> <substring of the test name> <tab> <reason>.
# The first matching rule wins, so order is significance: put the narrow ones first. A narrow rule
# placed after a broad one that also matches is dead, and dies quietly — `object_raw_response_headers`
# sat below `object_raw` for a milestone, classified as a defect and counted as out of scope.
#
# Status is one of:
#   out-of-scope  — bochka does not have this and the backlog says it will not (README, "What
#                   bochka is not"). Failing it is the design, not a defect.
#   deferred      — in scope eventually, nobody has claimed it. Becomes `defect` when it does.
#   defect        — bochka should pass this and does not. Every one of these names the task.
#
# A case matching nothing is reported as `unclassified`, which is the point of the file: a
# failure that nobody has looked at must not be able to hide inside a category.

# Шифрование распалось надвое в M26, и одна широкая строка на всю семью стала враньём: ключом
# клиента сервер теперь шифрует, ключом сервера — по-прежнему нет. Узкие правила стоят выше общего,
# иначе они мертвы — этот файл уже ловил такое с другой стороны (`object_raw_response_headers`).
# Повторная отправка части: у зашифрованной части ETag считается по шифротексту, а IV у каждой
# отправки свой, поэтому та же часть, посланная второй раз, получает другой ETag — и завершение,
# несущее ETag первой отправки, отвергается. Чинится тем, что ETag зашифрованной части считается
# по **открытому** тексту; это решение про семантику ETag у всех зашифрованных объектов, и его
# нельзя принимать походя (M-190а).
deferred	multipart_sse_c_get_part	повторная отправка части даёт другой ETag: у каждой отправки свой IV, а ETag считается по шифротексту (M-190а)
out-of-scope	sse_c_deny_algo_with_bucket_policy	кейс ставит политику бакета; политики — M29, а не шифрование
out-of-scope	sse_c_enforced_with_bucket_policy	кейс ставит политику бакета; политики — M29, а не шифрование
out-of-scope	_enc[	шифрование ключом сервера: сервер начинает хранить ключи, а это управление секретами (ресёрч покрытия)
out-of-scope	sse	шифрование ключом сервера (SSE-S3, SSE-KMS): сервер начинает хранить ключи (ресёрч покрытия)
out-of-scope	encryption	шифрование ключом сервера: сервер начинает хранить ключи (ресёрч покрытия)
# Versioning arrived in M17, so "one version per key by design" stopped being true and stopped
# being a reason. What is left of the family is three different things, and lumping them under one
# stale line would hide all three.
out-of-scope	bucket_logging	bucket logging is out of the v1 scope; these name a version only because their fixture does
# Six cases, and the combination they ask for exists in no bucket type S3 has. The model puts
# `x-amz-if-match-last-modified-time` and `x-amz-if-match-size` on `DeleteObject` with "only
# supported for directory buckets", and puts on the same operation's `VersionId` "for directory
# buckets ... only the `null` value of the version ID is supported". A condition that exists only
# where a named version does not is not a form anybody can send. The suite says as much itself:
# every one of the six carries `@pytest.mark.fails_on_aws # only supported for directory buckets`.
# Decided in M-159, and it returns if a source says the two can meet.
out-of-scope	_version_if_match	a condition on a named version: the model allows the conditions only for directory buckets, which allow only the null version (M-159)
# Read as `deferred` for two milestones on the grounds that "the version half is ours". It is,
# and it buys nothing: the operation these cases call is `GetObjectAcl`, and an ACL document
# from a server with no owner model is a fiction whichever version it is about. Routing
# `?versionId` into an answer that cannot be true is worse than not answering. Decided in M-160,
# and it returns with ACLs or not at all.
out-of-scope	versioned_object_acl	ACL по версии: сама ACL с M27 есть, а `?versionId` у `GetObjectAcl`/`PutObjectAcl` — нет; операция отвечает про текущую версию (M-160)
# Four rules stood here for M-161…M-163 and matched nothing at all: the cases they named have
# passed since those tasks closed. A rule that cannot fire is the trap this file already learned
# once from the other end — a narrow rule shadowed by a broad one — and it is worse in this
# direction: it claims a defect that no longer exists, and the claim is only read by whoever is
# looking for something else. Removed when the tails were closed.
# Both cases want a `404` for an absent key to carry `x-amz-delete-marker: false`, and the model
# refuses that in writing: `HeadObjectOutput.DeleteMarker` is documented "Specifies whether the
# object retrieved was (true) or was not (false) a Delete Marker. **If false, this response header
# does not appear in the response.**" So the header the cases read is one S3 does not send, and
# passing them means emitting it. Note what the lifecycle half of the second one proves: the
# marker does expire — the case then fails on the header, not on the expiry. Decided in M-165.
out-of-scope	delete_marker_nonversioned	the case reads x-amz-delete-marker: false off a 404, and the model says that header does not appear when false (M-165)
# Its name says lifecycle nowhere, which is how a broad `delete_marker` rule once picked it up and
# called it a versioning feature — two wrong readings of one case, from two directions.
out-of-scope	delete_marker_expiration	same header as above; the marker does expire, and the case fails after that (M-165)
# Lifecycle arrived in M23, so the blanket rule that stood here is gone. What is left is one
# family, and it fails for a reason that is a decision rather than a gap — measured, not guessed:
# with BOCHKA_LIFECYCLE_DAY_SECONDS at the suite's five, the same shortened day that makes the
# expiration cases observable also shortens what `x-amz-expiration` advertises, so the header says
# "five seconds" where the case wants "one day". Run the same four with S3TESTS_LC_DAY=86400 and
# all four pass. The suite pins the other arrangement — header in calendar days, deletion on the
# debug clock — under which the server deletes an object **before** the date it advertised, and
# that is the one guarantee the header gives.
out-of-scope	lifecycle_expiration_header	the advertised expiry-date follows the same clock the sweep does; the case wants them to disagree
# M27 сделал модель доступа: владелец у бакета и версии, шесть canned-имён, и всё это
# **исполняется**. Одна широкая строка «ACL вне рамок» после этого стала враньём того же рода,
# что и правило, которое не может сработать: она объясняла бы отсутствием того, что есть. Узкие
# правила стоят выше общего и называют то, чего действительно нет, — гранты по имени, анонимный
# доступ, блок публичного доступа, политики.
#
# Гранты — это решение, а не недоделка: грант называет **пользователя**, а у сервера есть ключи
# доступа и нет таблицы пользователей. Принятый и неисполняемый грант обнаруживают утечкой.
out-of-scope	acl_grant	гранты по имени пользователя или группы: у сервера ключи, а не пользователи (M27, решение)
out-of-scope	header_acl_grants	то же самое заголовками `x-amz-grant-*`
out-of-scope	acl_revoke	снятие всех грантов — это язык грантов, которого здесь нет
out-of-scope	tags_acl_public	кейс раздаёт права группе грантом; группы — часть ACL, которой здесь нет
out-of-scope	acl_bucket_policy_status	`PolicyStatus` считает публичность по политике бакета; политики — M29
out-of-scope	ignore_public_acls	блок публичного доступа — отдельная возможность, не ACL
out-of-scope	cors_presigned_get_object_tenant_v2	the case presigns with SigV2, which this server does not verify
out-of-scope	cors_presigned_get_object_v2	the case presigns with SigV2, which this server does not verify
out-of-scope	cors_presigned_put_object_tenant_v2	the case presigns with SigV2, which this server does not verify
out-of-scope	cors_presigned_put_object_v2	the case presigns with SigV2, which this server does not verify
out-of-scope	cors_presigned	кейс ходит по presign-ссылке с чужого origin без подписи; анонимный доступ — M28
out-of-scope	object_acl	что осталось у объекта после M27 — гранты по имени: `READ_ACP`, `WRITE_ACP` и `FULL_CONTROL` конкретному ключу
out-of-scope	acl	что осталось от ACL после M27 — гранты: canned-имена и владелец есть и исполняются (§3.6 контракта)
out-of-scope	policy	bucket policies: the access keys here are static (open question 4)
out-of-scope	_iam	IAM is a different service; this store authenticates with static keys
out-of-scope	_sts	STS is a different service; this store authenticates with static keys
out-of-scope	public_access_block	public access blocks belong with policies, which are out of scope
out-of-scope	block_public	public access blocks belong with policies, which are out of scope
out-of-scope	anonymous	анонимный доступ — слой 2 модели доступа (M28); ACL для него уже есть, разрешения ещё нет
out-of-scope	website	static website hosting is out of the v1 scope
# CORS is implemented (M14), and the eleven cases under this name fail for three different reasons,
# none of them "we do not have CORS". Splitting them is the point of this file.
out-of-scope	cors_origin	the case fetches across origins without a signature; anonymous access needs public-read ACLs
out-of-scope	notification	bucket notifications are out of the v1 scope
out-of-scope	replication	replication needs more than one node, which is the whole niche this is not
out-of-scope	logging	server access logging is out of the v1 scope
out-of-scope	request_payment	requester pays is out of the v1 scope
out-of-scope	storage_class	one storage class; there is one disk
out-of-scope	_torrent	BitTorrent retrieval was removed from S3 itself
out-of-scope	usage	usage accounting is a ceph extension, not S3
out-of-scope	post_object_anonymous	an anonymous form needs a bucket open to everyone, which needs public ACLs
out-of-scope	post_object_tags_anonymous	an anonymous form needs a bucket open to everyone, which needs public ACLs
out-of-scope	post_object_set_success_code	the form carries no policy at all; it is an anonymous POST to a public bucket
out-of-scope	post_object_set_invalid_success_code	the form carries no policy at all; it is an anonymous POST to a public bucket
out-of-scope	post_object_success_redirect_action	the upload succeeds; the case then reads the redirect target unsigned, which needs public read
out-of-scope	select	S3 Select is out of the v1 scope
out-of-scope	restore	Glacier restore is out of the v1 scope
out-of-scope	ownership	`ObjectOwnership` — отдельная возможность, а не ACL: она отключает ACL целиком, и здесь её нет
out-of-scope	tenant	multi-tenancy is a ceph extension, not S3
out-of-scope	account	account-level operations are out of the v1 scope
out-of-scope	user_policy	IAM user policies are a different service
out-of-scope	bucket_logging	server access logging is out of the v1 scope

# --- matched by name, and these are about something the name does not say ----------------------
#
# Classification reads the **test name**, and a name is not always what a test is about: every
# rule below names a case whose fixture sets up versioning or an ACL before it gets to the thing
# the name mentions. They spent a milestone filed as `deferred` conditional writes because
# `if_match` matched their names first, and the reason they carried was wrong twice over — the
# feature was implemented, and it is not what they exercise.
out-of-scope	_current_object_if	the fixture enables versioning first ("current object" is the versioned one); versioning is out of scope
out-of-scope	_current_if_match	the fixture enables versioning first; versioning is out of scope
out-of-scope	object_current_if	the fixture enables versioning first; versioning is out of scope
out-of-scope	_current_object	the fixture enables versioning first; versioning is out of scope
out-of-scope	list_multipart_upload_owner	the fixture sets an ACL; the owner needs an owner model anyway

# --- in scope and not done. `deferred` is a task: it is either in BACKLOG.md or this file has
# --- quietly become a second backlog that only its author knows about ---------------------------
#
# M13 emptied this section down to two, and both of them are here because the **decision** is not
# made rather than because the work is not done. Each one names what would settle it, because a
# `deferred` with no criterion is a `deferred` for ever.
deferred	object_read_unreadable	the premise of the original task was wrong and that was the finding: the key arrives as %C2%AE%C2%8A-, which is well-formed UTF-8, so this is not a malformed key but a key holding C1 control characters, and S3 refuses it at the URI with "Couldn't parse the specified URI." Refusing those here contradicts a documented decision (Р2: keys are not restricted, and encoding-type=url exists because a listing must carry bytes XML cannot). Settled by a source that says which bytes are invalid in a request line, not by one case
deferred	unicode_metadata	measured rather than guessed, and the earlier reason here was wrong: botocore does send non-ASCII metadata, as UTF-8, and reads the response back as latin-1. Passing this case means decoding the value as UTF-8 on the way in and re-encoding it as latin-1 on the way out, which makes metadata lossy above U+00FF — today it round-trips byte for byte. Settled by a source on what S3 stores, not by one case that only pins the asymmetry of one client

out-of-scope	not_owned	кейс раздаёт права грантом по имени; владелец с M27 есть, грантов нет
out-of-scope	owner_enforced	`BucketOwnerEnforced` отключает ACL целиком; такой настройки здесь нет
out-of-scope	owner_preferred	`BucketOwnerPreferred` меняет владельца записанного объекта; такой настройки здесь нет
out-of-scope	object_writer	`ObjectWriter` — это `ObjectOwnership`, которого здесь нет
out-of-scope	bucket_head_extended	x-rgw-object-count is a ceph extension, not S3
out-of-scope	unordered	allow-unordered is a ceph extension, not S3
out-of-scope	public_block	public access blocks belong with policies
out-of-scope	obj_public	публичный объект читают без подписи, а неподписанный запрос — это M28
out-of-scope	100_continue	the case sets a public-read ACL first; Expect: 100-continue itself is covered by the live-client harness
out-of-scope	multi_object_delete_key_limit	the case deletes more than a thousand keys in one request, which the protocol does not allow; bochka now answers MalformedXML instead of closing the connection, and the case expects it to succeed
out-of-scope	get_location	the case compares the region against the suite's own api_name setting
out-of-scope	bucket_list_return_data	the case reads an object ACL to compare owners

out-of-scope	object_raw	the "raw" family builds its own unsigned or ACL-dependent requests; anonymous access needs public-read ACLs
out-of-scope	object_anon	anonymous writes need public-write ACLs
out-of-scope	obj_enc_conflict	contradictory server-side-encryption headers; encryption is out of scope
# `bucket_gone` matched four test names and the reason explained one of them, so
# `test_atomic_write_bucket_gone` — a write into a bucket deleted mid-upload, which answered 200 for
# an object that was then gone — spent its life filed as "out of scope" (M-220, M-221). It only ever
# failed over TLS, where aws-cli frames the body with a trailer, so nobody had a reason to read the
# line. Named in full now rather than sharing a substring with a defect — and only one line is
# left of the four: `test_object_raw_get_bucket_gone` is already explained by the `object_raw`
# rule above, so a second one for it would be dead on arrival, which is the trap this file's
# header describes.
out-of-scope	object_delete_key_bucket_gone	an anonymous caller is refused before the missing bucket is looked for, so 403 arrives where the case wants 404
out-of-scope	object_gone	the case reads under an anonymous client and expects 404 before the 403
out-of-scope	ifnonmatch	conditional writes, as above
out-of-scope	upload_part_copy	UploadPartCopy, as above

# --- defects: bochka should pass these and does not. Each names what to do. --------------------
#
# Empty, and it was not empty by accident before: the one line here named
# `object_raw_response_headers`, which the broad `object_raw` rule above matches first — so it
# could never fire, and the case it was about was counted out of scope for a milestone. A narrow
# rule placed **after** a broad one is a rule that does nothing, and nothing says so. That is the
# same trap as reading the test name instead of the message, one level up: here the classification
# was right and unreachable.
#
# (The case itself is fixed. It was never about a content type set at upload — that was a
# misreading of `assert 'binary/octet-stream' == 'foo/bar'`. The client asked for the type in the
# query, `response-content-type`, and this server ignored the parameter. M-79.)
