# The other half of the auth decision: a Secret the operator already has, and a chart that creates
# none. This is also the shape in which the chart can prove nothing about the keys themselves —
# which is what `helm test` is for.
auth:
  existingSecret: bochka-keys
  existingSecretKey: bochka-keys.txt
