# Приёмка: `ceph/s3-tests`

Единственное число этого проекта, которое что-то значит **снаружи** его репозитория. «Сколько
`ceph/s3-tests` проходит однопроцессное JVM-хранилище» сравнимо с другими реализациями; ни один
бенчмарк, который bochka могла бы написать себе сама, таким свойством не обладает (Р6).

```bash
./ci/s3-tests.sh
```

Подмножество с трассировками — `BOCHKA_S3TESTS_K='bucket_list or ranged' ./ci/s3-tests.sh`.

```bash
S3TESTS_ANONYMOUS=1 ./ci/s3-tests.sh
```

Второе из двух чисел: харнесс поднимает сервер с `BOCHKA_ANONYMOUS=1`. Против **чужого** сервера
(`BOCHKA_S3TESTS_ENDPOINT`) переключатель бессмыслен и говорит об этом строкой — конфигурацию там
выбирает развёртывание, а не тот, кто запускает сьют.

## Оценить развёрнутый сервер, а не запущенный скриптом

```bash
BOCHKA_S3TESTS_ENDPOINT=s3.example.com:443 BOCHKA_S3TESTS_TLS=1 ./ci/s3-tests.sh
```

Тогда число включает **всё, что стоит между сьютом и сервером**, и в этом смысл переключателя,
а не в удобстве: прокси, который переписывает заголовок, режет тело или обрывает медленную
загрузку, двигает это число, и больше в репозитории его не двигает ничто. Один раз уже стоило пяти
зелёных тестов то, что `ETag` подменял nginx.

Две вещи харнесс за чужой сервер сделать не может, и обе на операторе:

* **длительность «дня» там обязана равняться `S3TESTS_LC_DAY` здесь.** Сьют меряет истечение
  ожиданием, поэтому против сервера с настоящими сутками семья `lifecycle` не меряется, а выглядит
  померенной. В чарте это `lifecycleDaySeconds` (плюс `lifecycleDayShortenedFor`, где написано,
  зачем) — значение, которое **удаляет данные раньше срока ровно во столько же раз**, поэтому оно
  и потребовало отдельного поля с отказом вместо `extraEnv` (M-218). Если день не укорочен, прогон
  печатает это отдельной строкой, а не оставляет тринадцать кейсов в `unclassified`;
* **анонимный доступ там тоже свой.** Значение `anonymous` в чарте не связано с
  `S3TESTS_ANONYMOUS` здесь: четыре кейса разойдутся, и в классификации они окажутся под
  `off-by-default` с причиной, называющей рубильник, — то есть строка скажет, чего не хватает,
  а не соврёт про отсутствие функции;
* три ключа сьюта (`s3main`, `s3alt`, `s3tenant`) должны существовать на том сервере. Прогон
  против стора с другими ключами — это несколько сотен ошибок в фикстурах, а не низкий счёт.

**Число развёртывания и число CI не сравнимы.** Первое снято на другой машине, с другим сервером
и без укороченного «дня». Сравнивать можно только два прогона на одном стенде: базовый — прямо
в сервис, и второй — сквозь то, что перед ним. Так и был найден весь M30: 455 против 433, и
двадцать два кейса пофамильно ([docs/measurements.md](measurements.md), M-212).

## Число

> **518 из 744 (70 %)**, ревизия сьюта `5522d1c`, 0 ошибок.
>
> **533 из 744 (72 %)** — то же самое с `BOCHKA_ANONYMOUS=1`.

**Чисел два, потому что число измеряет конфигурацию, а не код.** Анонимный доступ (M28) выключен
по умолчанию, поэтому прогон «как поставляется» его не видит вовсе. Заголовочное число — то,
что получает тот, кто просто запустил bochka; второе снимается тем же харнессом
(`S3TESTS_ANONYMOUS=1 ./ci/s3-tests.sh`) и говорит, чего стоит рубильник. Одно число с оговоркой
было бы враньём в обе стороны сразу.

**И прогон говорит, что он мерил.** Рядом со счётом печатаются нагрузка машины и время самого
долгого кейса вместе с запасом до потолка — сейчас это 50.4 с против 60, то есть **16 %**, а не
двадцать, как считалось. Прогон, в котором хоть один кейс вышел за клок, числом не считается
вовсе: скрипт называет такие кейсы поимённо и завершается ненулём. Это не строгость ради
строгости — загруженная машина один раз дала **228 из 744 вместо 426**, со 176
непроклассифицированными провалами, и целый час это читалось как регресс.

Потолок назван один раз (`CASE_TIMEOUT`, переопределяется `S3TESTS_CASE_TIMEOUT`) и уходит и
в pytest, и в проверку результатов; у самой проверки есть `--self-test`, который гоняется в начале
каждого прогона, потому что сторож, переставший узнавать формулировку `pytest-timeout`, выглядит
точно как прогон без таймаутов.

**Рядом с процентом всегда печатается, сколько тестов выполнилось.** Растущий счёт и сжимающийся
сьют выглядят одинаково, и единственная защита от второго — публиковать оба числа.

История по вехам: 96 → 109 (M5) → 149 (M6) → 167 (M7) → 222 (M10) → 244 (M12) → 274 (M13) →
283 (M14) → 315 (M16) → 343 (M17) → 379 (M18) → 383 (хвосты) → 415 (M23) → 424 (M24) → 426 (хвосты) → 431 (M26, SSE-C) → 465 (M27, модель доступа) → 466 (M-190а, ETag зашифрованной части) → 466 как поставляется и 474 с рубильником (M28, анонимный доступ) → 490 (M29, политики бакета) → 504 как поставляется и 514 с рубильником (M-204, закрывающий прогон M29) → **518 как поставляется и 533 с рубильником (M-225, M-227, M-228)**. M15 не двигала число вовсе,
и это было записано в ней заранее.

## Почему падает остальное

Голый процент говорит, сколько осталось, и ничего — из чего это состоит. А разница между «здесь
нет версионирования и не будет» и «здесь дефект» и есть всё расстояние: работать имеет смысл
ровно над вторым.

Классификация — [`ci/s3-tests-scope.txt`](../ci/s3-tests-scope.txt), по правилу на строку:
`<статус> ⇥ <подстрока имени теста> ⇥ <причина>`. Считает и печатает
[`ci/s3_tests_scope.py`](../ci/s3_tests_scope.py), запускается вместе со сьютом.

| Статус | Сколько | Что значит |
|---|---|---|
| `out-of-scope` | 213 | bochka этого не имеет, и в `README` записано, что не будет. Провал — это замысел, а не дефект |
| `off-by-default` | **11** | bochka это умеет, а измеренная конфигурация не включает. Причина обязана называть рубильник |
| `deferred` | **2** | и обе — про решение, которое не принято, а не про работу: бэклог пуст |

**С рубильником та же таблица — 209 вне рамок, 2 отложенных и `off-by-default` ноль**, что и есть
проверка самого статуса: строка, обещавшая «проходит, если включить», обязана исчезнуть, когда
включили. Одна не исчезла — `test_post_object_set_success_code` падал и там, — и это оказался
дефект, а не ярлык: `<PostResponse>` уходил с пространством имён, а кейс читает
`ET.fromstring(...).find('Key')`. Свой тест на этот документ у нас был и увидеть этого не мог: он
сравнивал **подстроку** `"<Key>report.txt</Key>"`, а подстрока слепа к тому, что объявляет
корневой элемент. Мы читали ответ как символы, клиент читает его как XML.
| `defect` | **0** | и на этот раз ноль проверенный, а не подразумеваемый |
| `unclassified` | **0** | ← ради этой строки всё и устроено |

**Таблица снята до M-227, M-228 и M-225, и все три вехи её сдвинули — а числа не перемеряны.**
`PublicAccessBlock` и `GetBucketPolicyStatus` сделаны и исполняются, POST-форма без политики
доходит до ACL, поэтому из классификации ушли правила `deferred` про них — включая
`public_access_block`, которое **не подбирало ни одного кейса** всё время, пока стояло: такого
имени в сьюте нет. Ушла и строка `out-of-scope` про `test_ignore_public_acls`, чья причина
«это отдельная возможность, не ACL» перестала быть причиной ровно тогда, когда возможность
появилась. Взамен добавились строки `off-by-default` — кейсы, которые читают **без подписи**
и упираются в `BOCHKA_ANONYMOUS` раньше, чем в проверяемый ими механизм.

Прогон, который превратит это в числа, — M-204; до него `deferred` **26**, `off-by-default` **4**
и `out-of-scope` **224** описывают конфигурацию, которой больше нет, и это сказано здесь, а не
подразумевается.
**Строк с этим статусом стало восемь, и четыре новых пришли из M-225.** POST-форма без политики
вовсе — это анонимная форма, и разрешает её ACL бакета, а не отсутствие проверки; значит она
живёт за тем же рубильником, что и остальной второй слой. Две из четырёх называются так, что
читаются про другое (`post_object_set_success_code`, `post_object_set_invalid_success_code`
выглядят кейсами про `success_action_status`, работающий с M16) — причина у всех четырёх одна.
Число «как поставляется» от этого не двигается вовсе; **целевое** число с рубильником — 502,
и подтверждает его прогон CI, а не этот абзац.

**Четвёртый статус завела M28, и завела по необходимости.** Анонимный доступ выключен
по умолчанию: кейсы, которые сервер проходит с `BOCHKA_ANONYMOUS=1`, в прогоне
«как поставляется» падают — и это не «вне рамок» (функция есть) и не «отложено» (делать нечего).
Без отдельного статуса они легли бы в `unclassified`, то есть в категорию «никто не смотрел»,
хотя смотрели. Причина такой строки обязана называть **имя настройки**: тогда её можно проверить,
перевернув рубильник, а не поверив автору.

**Дефектов было ноль, и ноль был неправдой.** Все девять нашлись в M23 не прогоном, а тем, что
у правил перечитали **причины**: `cors`, `tagging` и `object_lock` объясняли провалы словами
«этого в v1 нет» — про фичи, сделанные в M14 и M18. Двадцать девять кейсов сидели под ярлыком,
переставшим быть верным, и восемь из них оказались настоящими дефектами. Все девять закрыты
в M24, и три из них были про то, что защита не защищает: замок, снимаемый сменой режима, замок
многочастной загрузки, терявшийся между стартом и завершением, и preflight, разрешавший
незаявленные заголовки.

**Разница между этим нулём и прошлым — в том, как он получен.** Прошлый значил «правил со
статусом `defect` в файле нет»; этот значит «каждая строка файла перечитана и объясняет тот
провал, который под ней лежит». Первое достигается удалением строки.

**Ярлык «вне рамок» на дефекте хуже, чем `unclassified`.** Непроклассифицированное видно и просит
внимания; «вне рамок» выглядит закрытым вопросом и не просит ничего. Отсюда правило, которого тут
раньше не было: **веха, вводящая фичу, обязана перечитать строки этого файла, объясняющие провалы
её отсутствием** — они не перестают срабатывать сами, они начинают врать.

**`deferred` — это задачи, и они обязаны быть в бэклоге.** Иначе этот файл тихо становится вторым
бэклогом, о котором знает только тот, кто его писал: `BACKLOG.md` какое-то время сообщал, что
делать нечего, пока здесь лежало 43 строки «в рамках, никем не взято». Вопрос «почему бэклог пуст,
если сьют 32 %» и был про это.

**И `deferred` без критерия — это `deferred` навсегда.** Обе оставшиеся строки говорят не «руки
не дошли», а «решение не принято», и каждая называет, чем оно принимается: источником, который
скажет, какие байты недопустимы в строке запроса, и источником про то, что S3 хранит
в метаданных. Один кейс сьюта таким источником не является — он фиксирует поведение одного
клиента, а не правило.

**Остальные одиннадцать закрылись за один заход, и почти все — источником.** Модель сказала про
условия удаления «only supported for directory buckets», про заголовок надгробия — «if false,
this response header does not appear»; ни то, ни другое не требовало работы, только вопроса.
Строки прожили в `deferred` по нескольку вех потому, что их перечитывали как формулировку,
а не как утверждение, которое можно проверить.

**Заодно ушли четыре правила, не срабатывавшие вовсе** — они называли дефекты M-161…M-163,
исправленные ещё тогда. Правило, которое не может сработать, — та же ловушка, что и правило,
затенённое широким, только с другой стороны: оно утверждает дефект, которого нет, и читает
его тот, кто искал что-то другое.

**Порядок правил — тоже значение, и правило ниже широкого не срабатывает никогда.** Строка
`defect object_raw_response_headers` стояла под `out-of-scope object_raw`, который подбирает то же
имя первым. Кейс целую веху считался вне рамок, а классификация, объявлявшая его дефектом, была
верна и недостижима.

**И правило читает имя теста, а тест бывает не про своё имя.** Из тех 43 тринадцать оказались вне
рамок: их фикстура включает версионирование или ставит ACL, но в имени этого нет — и шаблон
`if_match` подобрал их первым. Причина, которую они носили, была неверна дважды: и фича сделана,
и тест не про неё. Группу, вызывающую сомнение, надо смотреть по **сообщению**, а не по имени.

**`unclassified` — не остаток, а сторож.** Провал, на который никто не посмотрел, не должен уметь
спрятаться внутри категории: он попадает сюда и печатается по имени. Пустая категория «ноль» —
это утверждение, что каждый из 500 провалов кто-то классифицировал руками.

Если считать долю от того, что вообще в рамках, выходит 383 из 396 — **97 %**. Это число здесь
стоит **как предупреждение, а не как достижение**: знаменатель выбран мной, и растянуть его в свою
пользу ничто не мешает, кроме дисциплины, — а девяносто девять процентов от собственного списка
рамок можно получить, просто расширив список. **51 % честнее ровно тем, что его выбрал не я.**
Смотреть надо на первое число и на то, куда оно движется.

### Вне рамок, крупным планом

Пересчитано после M27, и таблица переписана целиком, а не поправлена: прежняя была времён M13
и держала строки «версионирование — одна версия на ключ по замыслу», «lifecycle», «POST-форма» —
про возможности, сделанные в M17, M23 и M16. Такая таблица врёт ровно тем способом, который этот
файл и заводился ловить, и живёт долго, потому что её читают редко.

| Сколько | Что |
|---|---|
| 101 | серверное шифрование (SSE-S3, SSE-KMS): сервер начинает **хранить ключи**, а это управление секретами |
| 41 | политики бакетов и всё, что их читает, — `PolicyStatus` в том числе (M29) |
| 39 | логирование бакета: без политик оно не даёт ни одного кейса, поэтому идёт за ними (M29) |
| 29 | **гранты** — права по имени пользователя или группы; canned-имена и владелец с M27 есть и исполняются |
| 28 | анонимный доступ и всё, что его требует: «сырое» семейство, presign с чужого origin, POST-форма без подписи (M28) |
| 8 | `ObjectOwnership` — отдельная возможность, а не ACL. **Половина строки устарела в M-227**: public access block здесь был вместе с ним, и теперь он есть; сколько из восьми осталось, скажет M-204 |
| 6 | условия на именованную версию: модель разрешает их только directory-бакетам (M-159) |
| 6 | расширения ceph (`allow-unordered`, `x-rgw-*`, usage) и SigV2 в presign |
| 6 | фикстура включает версионирование там, где тест не про него |
| 12 | остальное поштучно, каждое со своей причиной в `ci/s3-tests-scope.txt` |

**Строк с причиной «ACL вне рамок» здесь больше нет**, и это часть работы M27: правило, которое
объясняет провал отсутствием того, что уже есть, — та же ложь, что и правило, которое не может
сработать. Шесть правил, переставших ловить хоть что-нибудь, из файла удалены; остальные
переписаны так, чтобы называть настоящую причину — гранты, анонимный доступ, `ObjectOwnership`.

**Одно из шести было `deferred`, и это отдельный случай.** Строка обещала, что POST-форма не
читает ключ SSE-C из полей, — кейс `test_encryption_sse_c_post_object_authenticated_request`
проходит, то есть читает. `deferred` без задачи в бэклоге и без кейса, который бы её просил, —
это второй бэклог, о котором знает только автор строки; ровно то, что этот файл про себя уже
однажды выяснил.

### Что нашла M13

Разбор тридцати кейсов, и половина строк, которыми они были заведены, оказалась неверна —
это главный её результат. Подробный разбор — в [BACKLOG.md](../BACKLOG.md), итог M13; сюда
вынесено то, что относится к самой приёмке.

**Задача, написанная по сообщению, прочитанному в одну сторону.** M-85 гласила «предусловие на
отсутствующем ключе отвечает `404` вместо `412`». Наоборот: отвечал `412`, а S3 отвечает `404`.
Никто не перечитал сообщение до того, как начать чинить; первый же красный тест сказал правду
за секунду.

**«Не воспроизведено» стоило двух строк чтения.** Над вторым вызовом в сьюте стоит комментарий
`# check extra client.complete_multipart_upload`: завершение вызывается дважды, и это делает любой
SDK, у которого оборвалось соединение.

**Ещё одна задача была не про то, что в ней написано.** M-79 читалась как «`Content-Type`,
заданный при загрузке, вернулся умолчанием» — потому что сообщение
`assert 'binary/octet-stream' == 'foo/bar'` именно так и выглядит. Тип при загрузке не задавался:
клиент просил его в query, `response-content-type`.

**Таймаут стенда мерил стенд.** `test_multipart_get_part` уперся в двадцать секунд ровно тогда,
когда наконец стал что-то скачивать. Замер развёл, чьё это время: сервер делает всю работу теста
за **0.32 с**, а цикл сравнения внутри самого теста — `data = data[len(chunk):]` по строке в
16 МиБ на каждый килобайт — стоит **11.57 с**. Порог поднят до шестидесяти секунд, и это записано
в скрипте вместе с числами.

**Сторож, до которого не доходит управление, — не сторож.** Проверка «ничего не выполнилось — это
провал, а не ноль» стояла внизу скрипта. Правка комментария поставила апостроф внутри
одинарно-кавыченной строки, остаток исполнил хозяйский шелл, `set -u` убил скрипт — и он
завершился, не напечатав ни числа, ни жалобы. Проверка переехала в `trap EXIT`.

### Дефекты

**Ноль.** Все три, которые нашла M10, закрыты — и два из трёх закрылись не так, как были
сформулированы.

**M-77** оказался тем, что стоило всей вехи: canonical request — это байтовая строка, а хешировался
он как UTF-8, то есть каждый байт выше 0x7F удваивался. Это пережило 34 официальных вектора, оба
режима подписи и четыре живых клиента — потому что всё, что они подписывают, ASCII.

**M-78 был сформулирован из неверной посылки.** «Ключ, не являющийся UTF-8» приезжает как
`%C2%AE%C2%8A-`, то есть правильным UTF-8: кейс про управляющие символы C1, а не про кодировку.
Отвергать их — против Р2, так что кейс переведён в `deferred` с причиной, а не подогнан.
Проверка на корректность UTF-8 при этом была настоящей дырой и сделана: **порядок** ключей
задаётся байтами, **допустимость** — UTF-8.

**M-79** переведён в `deferred`: проверка через свой стенд показала, что нынешний botocore
отказывается отправлять не-ASCII метаданные вообще, так что чему тут соответствовать —
не установлено.

## Что приёмка нашла такого, чего не нашли девять вех до неё

**`CopyObject` не существовало.** 84 кейса, 11 % сьюта, и это то, чем работает
`aws s3 cp s3://a s3://b`. Вехи придумывались из модели операций, а приёмка — из того, чем
пользуются, и разошлись они ровно здесь.

**Тесты падали в чужой уборке.** `ListVersionsResult` без `NextKeyMarker` ронял пагинатор botocore
внутри `nuke_prefixed_buckets`, а её сьют гоняет **вокруг каждого теста**. Десятки кейсов,
не имевших отношения к версионированию, падали в teardown и проходили поодиночке — то есть
выглядели как флаки стенда. Один отсутствующий пустой элемент.

**Самосогласованная ошибка кодирования.** Листинг кодировал пробел как `+`; botocore декодирует
`unquote`, а не `unquote_plus`. Свой кодек кодировал и декодировал одинаково, поэтому все
собственные тесты проходили. Такое находит только клиент, который не разделяет ошибку.

**Непойманное исключение роняло соединение.** Клиент видел `ConnectionClosedError` —
неотличимое от сетевого сбоя, на котором любой SDK делает повтор.

**`412` без тела ломал разбор ответа** у клиента: статус ошибки без документа ошибки — это другая
поломка, а не более короткая.

## Учётные данные

Сьют требует трёх наборов — `main`, `alt`, `tenant` — и половину тестов не запускает без второго.
Все три заводятся `BOCHKA_KEYS` в [`ci/s3-tests.sh`](../ci/s3-tests.sh). Тесты IAM и STS
исключены статусом `out-of-scope` и с причиной: это другой сервис, а здесь ключи статические
(открытый вопрос 4 ресёрча).
